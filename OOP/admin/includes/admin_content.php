<div class="container-fluid">

    <!-- Page Heading -->
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">
                Blank Page
                <small>Subheading</small>
            </h1>
            <?php
            // W tym pliku wywołujeszc wszystko 
            // to jest po zalogowaniu i beda wywoływane

            // $result_set = User::find_all();
            // var_dump($result_set);
            // foreach ($result_set as $key) {
            //     echo $key->username;
            // }
            // $found_user = User::find_user_by_id(2);


            // $user = User::instantation($found_user);
            //echo $user->id;

            // $user = new User();
            // $user->username="Mosiek";
            // $user->save();
                echo INCLUDES_PATH;

            // $photos = new Photo();
            // $photos->title="Obrazek";
            // $photos->size=30;
            // $photos->save();
            // $photo = Photo::find_all();
            // foreach ($photo as $key) {
            //     echo $key->title;
            // }


            // $photo->title="Obrazek";
            // $photo->save();
            // $user->password="saaaaas";
            // $user->first_name="Damian";
            // $user->last_name="Mosidsadasński";
            // $user->create(); 

            // $user = User::find_user_by_id(6);
            // $user-> username="WHATEVER";
            // $user->update();
            // $user = new User();
            // $user->username="Straicer21";
            // $user->save();


            ?>
            <ol class="breadcrumb">
                <li>
                    <i class="fa fa-dashboard"></i> <a href="index.html">Dashboard</a>
                </li>
                <li class="active">
                    <i class="fa fa-file"></i> Blank Page
                </li>
            </ol>
        </div>
    </div>
    <!-- /.row -->

</div>